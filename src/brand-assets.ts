// Root-relative import uses the approved inline-asset loader in rollup.config.js.
// Nested views reuse this export rather than emitting unpackaged asset URLs.
export { default as brandIcon } from "./assets/regear-icon.svg";
